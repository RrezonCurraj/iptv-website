import Hero from '../components/Hero';

const Home = () => {
  return (
    <div>
      <Hero />
      {/* We will add Features, Pricing preview, etc. here later */}
    </div>
  );
};

export default Home;
